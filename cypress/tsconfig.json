// Extends the main tsconfig.json file to avoid clashing with the Cypress types
{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["cypress"]
  },
  "include": [
    "./**/*.ts"
  ]
}